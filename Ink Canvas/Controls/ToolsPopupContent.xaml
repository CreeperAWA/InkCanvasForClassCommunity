<UserControl x:Class="Ink_Canvas.Controls.ToolsPopupContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns:controls="clr-namespace:Ink_Canvas.Controls;assembly=InkCanvas.Controls"
             xmlns:i18n="clr-namespace:Ink_Canvas.MarkupExtensions"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="200">
    <Border CornerRadius="8" Background="{DynamicResource ToolsPopupBackground}"
            BorderBrush="#3b82f6" BorderThickness="2">
        <ikw:SimpleStackPanel Margin="-1">
            <Grid Margin="8,7,10,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="10"
                           Text="{i18n:I18n Key=Tools_MoreFeaturesTitle}"
                           Foreground="{DynamicResource ToolsPopupTitleForeground}" FontWeight="Bold"/>
                <ui:FontIcon x:Name="CloseIcon" Grid.Column="1" Icon="{x:Static ui:SegoeFluentIcons.ChromeCloseContrast}"
                             Foreground="#DC2626" FontSize="8" VerticalAlignment="Center" />
            </Grid>
            <Border Margin="6,0,6,6" BorderBrush="{DynamicResource ToolsPopupInnerBorderBrush}" Background="{DynamicResource ToolsPopupInnerBackground}" BorderThickness="1"
                    CornerRadius="4">
                <ikw:SimpleStackPanel Margin="2" Spacing="1">
                    <ikw:SimpleStackPanel.Resources>
                        <Style TargetType="Label" BasedOn="{StaticResource AutoFitToolPopupLabel8}" />
                    </ikw:SimpleStackPanel.Resources>
                    <ikw:SimpleStackPanel Margin="0,0,0,0" Height="40" Orientation="Horizontal">
                        <controls:ToolMenuButton x:Name="TimerToolBtn" Label="{i18n:I18n Key=Tools_Timer}" />
                        <controls:ToolMenuButton x:Name="RandomDrawToolBtn" Label="{i18n:I18n Key=Tools_RandomDraw}" />
                        <controls:ToolMenuButton x:Name="SingleDrawToolBtn" Label="{i18n:I18n Key=Tools_SingleDraw}" />
                    </ikw:SimpleStackPanel>
                    <ikw:SimpleStackPanel Margin="0,0,0,0" Height="40" Orientation="Horizontal">
                        <controls:ToolMenuButton x:Name="SaveToolBtn" Label="{i18n:I18n Key=Tools_Save}" />
                        <controls:ToolMenuButton x:Name="OpenToolBtn" Label="{i18n:I18n Key=Tools_Open}" />
                        <controls:ToolMenuButton x:Name="ReplayToolBtn" Label="{i18n:I18n Key=Tools_Replay}" />
                    </ikw:SimpleStackPanel>
                    <ikw:SimpleStackPanel Margin="0,0,0,0" Height="40" Orientation="Horizontal">
                        <controls:ToolMenuButton x:Name="ScreenshotToolBtn" Label="{i18n:I18n Key=Tools_Screenshot}" />
                        <controls:ToolMenuButton x:Name="ManualToolBtn" Label="{i18n:I18n Key=Tools_Manual}" />
                        <controls:ToolMenuButton x:Name="SettingsToolBtn" Label="{i18n:I18n Key=Tools_Settings}" />
                    </ikw:SimpleStackPanel>
                </ikw:SimpleStackPanel>
            </Border>
        </ikw:SimpleStackPanel>
    </Border>
</UserControl>
